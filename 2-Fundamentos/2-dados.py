"""As regras de sintaxe para
 variáveis em Python incluem
   que os nomes devem começar
     com uma letra ou um 
     underscore (_), podem 
     conter letras, números 
     e underscores, não podem 
     conter espaços ou 
     caracteres especiais, 
     e não podem ser palavras 
     reservadas da linguagem. 
     Além disso, Python diferencia 
     maiúsculas de minúsculas, 
     por isso minhaVariavel e 
     minhavariavel são diferentes. 
     É uma boa prática usar 
     o snake_case (letras 
     minúsculas separadas por 
     underscores) para nomes 
     descritivos e fáceis
     de ler
"""

#Tipos elementares de dados
#PythonFlix
name = "Top Gun Maverick"
releaseYear = 2023
movieScore = 9.5
planIncluded = False

print(name)
print(releaseYear)
print(movieScore)
print(planIncluded)

print(type(name))
print(type(releaseYear))
print(type(movieScore))
print(type(planIncluded))